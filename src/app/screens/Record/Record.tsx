/* eslint-disable react-native/no-inline-styles */
import React from 'react';
import Tabs from '../../navigators/MaterialTopTabNavigator';

const Record: React.FC = () => {
  return <Tabs />;
};

export default Record;
